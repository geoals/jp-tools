services:
  whisper:
    build: .
    container_name: yt-mine-whisper
    volumes:
      # Share audio files between host and container
      - ./audio:/app/audio
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
