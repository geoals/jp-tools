# The venv at ~/.local/share/kotodex/venv, which kotodex-capture runs the
# logger and the VAD from. `setup.sh` creates it and installs this file.
#
# PySide6 is deliberately not listed. The venv is built with
# `--system-site-packages` so the packaged PySide6 is visible through it, and
# only a distribution that has no such package gets a pip one — a pip wheel
# carries its own Qt and cannot load the system layer-shell plugin, which is
# what puts the overlay over a fullscreen game.

# vn-ws-logger.py — the Textractor WebSocket client
websockets

# vn-vad.py — silero VAD over the ring buffer
onnxruntime
numpy
